[package]
name = "p2panda-stream-next"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[features]
default = ["orderer"]
orderer = ["dep:thiserror"]

[dependencies]
thiserror = { version = "2.0.16", optional = true }

[dev-dependencies]
p2panda-core = { path = "../p2panda-core", version = "0.4.0" }
serde = { version = "1.0.219" }
tokio = { version = "1.47.1", features = ["macros", "rt"] }
